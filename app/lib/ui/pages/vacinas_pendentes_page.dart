import 'package:flutter/material.dart';

class VacinasPendentesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Vacinas Pendentes'),
      ),
      body: Center(
        child: Text('Conteúdo das Vacinas Pendentes'),
      ),
    );
  }
}
